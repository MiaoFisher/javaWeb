【面试常考】
sleep() wait()的异同
相同：
    (1)一旦执行该方法,都会让当前线程进入阻塞状态
不同:
    (1)两个方法声明的地方不同:sleep()是声明在Thread中，wait()是声明在java.lang.Object方法中
    (2)两个方法的使用位置不同:sleep()可以使用在任意地方，wait()必须使用在同步代码块(synchronized)中
    (3)是否会自动释放同步监视器:如果两个方法都是在同步方法代码块或者同步方法中
        sleep()在指定时间后会自动释放同步监视器,而wait()不会自动释放同步监视器
